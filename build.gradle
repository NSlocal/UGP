buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.1.0"
    }
}

// ❌ HAPUS SEMUA BLOK allprojects { repositories { ... } } — INI PENYEBAB ERROR!

task clean(type: Delete) {
    delete rootProject.buildDir
}
